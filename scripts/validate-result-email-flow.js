const fs=require('fs');const app=fs.readFileSync('app.js','utf8');const cfg=fs.readFileSync('config.js','utf8');const fail=[];const has=s=>app.includes(s)||cfg.includes(s);['dispatchPostSurveyCommunication','sendSurveyResultToClient','buildResultEmailPayload','recordCommunication','resendResultEmail','communicationsSection','/communication/result/send','Authorization'].forEach(k=>{if(!has(k))fail.push(`ausente: ${k}`)});if(!/EMAIL_TRANSPORT:'(external-api|disabled)'/.test(cfg))fail.push('EMAIL_TRANSPORT deve aceitar external-api/disabled');if(!cfg.includes('COMMUNICATION_GATEWAY_PUBLIC_TOKEN'))fail.push('token público MVP ausente');if(!/resultEmail\?\.status===['"]sent/.test(app))fail.push('deduplicação por status sent ausente');if(!/catch\(error\)[\s\S]{0,220}status:'failed'/.test(app))fail.push('falha do gateway não registrada/controlada');['score','maxScore','level','resultUrl','certificateUrl'].forEach(k=>{if(!app.includes(k))fail.push(`payload sem ${k}`)});if(!/participante não acessa|role==='participante'/.test(app))fail.push('participante deve ser bloqueado em comunicações');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-result-email-flow: PASS');
