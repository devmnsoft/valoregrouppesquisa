# Reparo de link público

Links públicos usam `publicToken`, `token` ou `accessToken`. `tokenHash` é aceito apenas como compatibilidade interna em Functions quando não há token público, nunca como token exibido ao participante.
