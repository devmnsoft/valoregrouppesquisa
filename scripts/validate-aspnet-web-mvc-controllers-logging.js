const fs=require('fs'),path=require('path'); let hits=[]; for(const f of fs.readdirSync('backend/Valora.Web/Controllers').filter(x=>x.endsWith('.cs'))){const s=fs.readFileSync(path.join('backend/Valora.Web/Controllers',f),'utf8'); ['ILogger<','try','catch','LogError'].forEach(x=>{if(!s.includes(x))hits.push(`${f}: ${x}`)})} if(hits.length){console.error(hits.join('\n'));process.exit(1)} console.log('validate-aspnet-web-mvc-controllers-logging: PASS');
