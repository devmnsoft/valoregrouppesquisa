const fs=require('fs');function r(f){return fs.readFileSync(f,'utf8')}const s=r('backend/Valora.Application/Services/PublicSurveys/PublicResponseTransactionService.cs');const all=s+'\n'+r('backend/Valora.Application/Contracts/Repositories/IResponseRepository.cs');const req=['BeginTransaction','Commit','Rollback','IDbTransaction','IDbConnection'];const miss=req.filter(x=>!all.includes(x));if(miss.length){console.error('Transação incompleta: '+miss.join(','));process.exit(1)}console.log('validate-transactional-public-submit: PASS');
