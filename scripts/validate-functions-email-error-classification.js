const fs=require('fs');const s=fs.readFileSync('functions/index.js','utf8');const b=s.slice(s.indexOf('function classifyEmailError'),s.indexOf('function normalizeSendMailResult'));for(const x of ['smtp_connection_failed','smtp_auth_failed','email_config_incomplete','Detalhe:','ESOCKET','ENOTFOUND','Greeting never received']) if(!b.includes(x)) throw new Error('classifyEmailError incompleto: '+x);console.log('functions email error classification: PASS');
