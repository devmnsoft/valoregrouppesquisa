document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('[data-page="login-page"] form');if(!form)return;form.addEventListener('submit',async event=>{event.preventDefault();const errorBox=document.querySelector('.error-state');errorBox.classList.add('d-none');if(!form.checkValidity()){form.classList.add('was-validated');return;}const button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Entrando…';try{await AuthApi.login({email:form.email.value.trim(),password:form.password.value});const authenticated=await AuthApi.me();if(!authenticated?.user)throw new Error('A sessão segura não pôde ser confirmada.');window.location.assign('/Dashboard');}catch(error){form.password.value='';errorBox.textContent=error.message||'Não foi possível entrar.';errorBox.classList.remove('d-none');errorBox.focus();}finally{button.disabled=false;button.textContent='Entrar';}});});
