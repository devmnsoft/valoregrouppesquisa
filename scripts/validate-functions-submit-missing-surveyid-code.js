const fs=require('fs');const f=fs.readFileSync('functions/index.js','utf8');['function publicSubmitPayloadFromRequest(req)','const data=publicSubmitPayloadFromRequest(req)','data.surveyId||data.survey||data.id','code:\'missing_survey_id\'','friendlyMessage'].forEach(x=>{if(!f.includes(x)){console.error('functions missing '+x);process.exit(1)}});console.log('validate-functions-submit-missing-surveyid-code: PASS');
