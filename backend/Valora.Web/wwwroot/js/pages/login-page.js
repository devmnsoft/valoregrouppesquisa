$(function(){const form=document.querySelector('[data-page="login-page"] form'); if(!form)return; form.addEventListener('submit',async e=>{e.preventDefault();$('.error-state').addClass('d-none').text(''); if(!form.checkValidity()){form.classList.add('was-validated');return;} try{Loading.show('Entrando...'); const result=await AuthApi.login({email:form.email.value.trim(),password:form.password.value}); if(result&&result.token)Session.save(result); Toast.success('Login realizado com sucesso.'); window.location.href='/Dashboard';}catch(error){form.password.value=''; const msg=formatFriendlyError(error); $('.error-state').removeClass('d-none').text(msg); Toast.error(msg);}finally{Loading.hide();}});});
