const fs=require('fs');const s=fs.readFileSync('app.js','utf8');const body=(s.match(/function buildRadar[\s\S]*?function buildSystemImbalance/)||[''])[0];function fail(m){console.error(m);process.exit(1)}if(!/score/.test(body)||!/maxScore/.test(body)||!/percentage/.test(body))fail('buildRadar data model incomplete');if(/bar\s*:|<span|radar-html-bar/.test(body))fail('buildRadar returns HTML/bar');console.log('validate-legacy-radar-data-model: PASS');
