const {read,assertIncludes}=require('./validate-public-result-empty-rt-common');const s=read('app.js');assertIncludes(s,'function isPublicResultAttemptRoute()','must define public result attempt route');assertIncludes(s,'params.has(\'result\')','must catch result param even with empty rt');assertIncludes(s,'renderIncompletePublicResultLink(p)','must render incomplete link before auth route');assertIncludes(s,"releasePublicUi('public_result_attempt_route')",'must release public UI for attempt route');console.log('OK result route catches empty rt');
