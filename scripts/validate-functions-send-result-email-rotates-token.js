const fs=require('fs');const s=fs.readFileSync('functions/index.js','utf8');const fn=s.slice(s.indexOf('exports.sendResultEmail=onCall'),s.indexOf('async function requireAdminUser'));for(const x of ['rotateResultTokenForResponse(response.id','send_result_email','buildPublicResultUrl(response.id,resultToken)','sendResultEmailInternal({responseId:response.id,resultToken'])if(!fn.includes(x))throw new Error('sendResultEmail sem rotação/url/token real: '+x);console.log('validate-functions-send-result-email-rotates-token: PASS');
