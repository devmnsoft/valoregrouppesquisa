#!/usr/bin/env node
const fs=require('fs');const path=require('path');const cfg=fs.readFileSync('config.js','utf8');const app=fs.readFileSync('app.js','utf8');const fail=[];function ok(c,m){if(!c)fail.push(m)}const gatewayDir=path.join(process.cwd(),'communication-gateway');if(fs.existsSync(gatewayDir)){const server=fs.readFileSync(path.join(gatewayDir,'server.js'),'utf8');['/health','/communication/status','/communication/result/send','/communication/email/send','/communication/whatsapp/send'].forEach(r=>ok(server.includes(r),`endpoint ausente no gateway: ${r}`));ok(/res\.status\([^)]*\)\.json|\.json\(/.test(server),'gateway deve responder erros em JSON');}
ok(/EXTERNAL_API_BASE_URL:\s*'https:\/\/api\.valoragroup\.mnsoft\.com\.br'/.test(cfg),'base URL externa de comunicação ausente');ok(app.includes('/communication/result/send')||app.includes('/communication/email/send'),'frontend não referencia endpoint transacional');ok(app.includes('WHATSAPP_TRANSPORT')||cfg.includes('WHATSAPP_TRANSPORT'),'transporte WhatsApp não configurado');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-gateway-integration: PASS');
