const fs=require('fs'); const compose=fs.readFileSync('docker-compose.yml','utf8'); ['valora-web','backend/Valora.Web/Dockerfile','5088:8080','Api__BaseUrl','WebApp__PublicUrl','depends_on'].forEach(t=>{if(!compose.includes(t)&&!fs.existsSync(t)) throw new Error(`Docker Web sem ${t}`)}); console.log('validate-aspnet-web-docker-smoke: PASS (estrutural; execute docker compose up para smoke runtime)');
