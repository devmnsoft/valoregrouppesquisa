# Primeiro SuperAdmin
Use `scripts/admin/create-superadmin.ps1` em terminal seguro. O script não registra senha; ele solicita hash gerado pelo utilitário oficial do projeto e imprime SQL para execução via `psql`. Nunca commite dados reais.
