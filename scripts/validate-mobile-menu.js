const fs=require('fs');const app=fs.readFileSync('app.js','utf8'),css=fs.readFileSync('style.css','utf8');const fail=[];function ok(r,m){if(!r)fail.push(m)}
ok(/function toggleMenu\(force\)/.test(app),'toggleMenu(force) ausente.');ok(/function closeMobileMenu\(\)/.test(app),'closeMobileMenu ausente.');ok(/function createActions\(\)[\s\S]*toggleMenu/.test(app),'toggleMenu não exposto em createActions.');ok(/function navigate\(path\)\{closeMobileMenu/.test(app),'navigate não fecha menu.');ok(/async function logoutUser\(\)\{closeMobileMenu/.test(app),'logout não fecha menu.');ok(/max-width:\s*860px[\s\S]*\.nav-actions\.open/.test(css),'CSS mobile 860px ausente.');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-mobile-menu: PASS');
