const {read,must}=require('./legacy-public-submit-validator-common');const f=read('firebase-repository.js');must('normalizeCallablePublicError exists',/function normalizeCallablePublicError/.test(f));must('details.code is prioritized',/detailCode\|\|rawCode/.test(f));must('known codes mapped',/participant_required[\s\S]*lgpd_required[\s\S]*required_question_missing[\s\S]*invalid_public_token/.test(f));
