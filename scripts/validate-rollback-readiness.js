#!/usr/bin/env node
const fs=require('fs'); const {assert,writeJson,writeMd}=require('./live-gate-utils');
['scripts/backup-local-postgres.js','scripts/restore-local-postgres.js','PRODUCTION_ROLLBACK_CHECKLIST.md','BUG_RISK_REGISTER.md'].forEach(f=>assert(fs.existsSync(f),`${f} ausente`));
const doc=fs.readFileSync('PRODUCTION_ROLLBACK_CHECKLIST.md','utf8'); ['backup','restore','DATA_PROVIDER=firebase','responsável','tempo estimado','risco','health pós-rollback'].forEach(x=>assert(new RegExp(x,'i').test(doc),`rollback item missing: ${x}`));
const report={generatedAt:new Date().toISOString(),status:'PASS',checks:['backup local script exists','restore local script exists','health pós-restore documented','DATA_PROVIDER=firebase documented','owner/time/risk documented'],residualRisk:'Backup/restore real exige Docker local ativo; este gate valida prontidão e scripts.'}; writeJson('reports/rollback-readiness-report.json',report); writeMd('ROLLBACK_READINESS_REPORT.md','Rollback Readiness Report',[`- Status: ${report.status}`,...report.checks.map(c=>`- PASS ${c}`),`- Risco residual: ${report.residualRisk}`]); console.log('validate-rollback-readiness: PASS');
