const fs=require('fs');const s=fs.readFileSync('app.js','utf8');for(const x of ['function radarBarPdfSafe','function buildRadar','function renderRadarHtml','function renderRadarBarHtml','percentage','maxScore'])if(!s.includes(x)){console.error('missing radar '+x);process.exit(1)}if(/buildRadar[\s\S]*bar\s*:/.test(s)){console.error('buildRadar must not store HTML bar');process.exit(1)}console.log('legacy radar ok');
