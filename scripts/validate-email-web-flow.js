const fs=require('fs');const files=['backend/Valora.Web/wwwroot/js/api/communications-api.js','backend/Valora.Web/wwwroot/js/pages/result-page.js','backend/Valora.Web/wwwroot/js/pages/communications-page.js'];const txt=files.map(f=>fs.readFileSync(f,'utf8')).join('\n');const fail=[];['sendResultEmail','/communications/result/','send-email','name=\'to\'','/admin/email-jobs','processEmailQueue','correlationId','failed-network','pending-retry'].forEach(x=>{if(!txt.includes(x))fail.push(`web sem ${x}`)});if(/SMTP.*Password|smtpPassword/i.test(txt))fail.push('UI expõe password SMTP');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-email-web-flow: PASS')
