const {read,must}=require('./legacy-public-submit-validator-common');const a=read('app.js'),c=read('style.css');must('withLoading exists',/async function withLoading/.test(a));must('setButtonLoading exists',/function setButtonLoading/.test(a));must('submit uses processing message',/Aguarde, estamos processando sua requisição/.test(a));must('overlay css exists',/global-loading-overlay/.test(c));
