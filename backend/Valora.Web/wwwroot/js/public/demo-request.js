document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('[data-demo-form]');if(!form)return;form.addEventListener('submit',async event=>{event.preventDefault();const feedback=form.querySelector('.form-feedback');if(form.website.value||!form.checkValidity()){form.reportValidity();return;}const button=form.querySelector('button');button.disabled=true;feedback.textContent='Enviando…';try{const payload={name:form.name.value.trim(),email:form.email.value.trim(),phone:form.phone.value.trim(),companyName:form.companyName.value.trim(),segment:form.interest.value,companySize:form.companySize.value,roleTitle:form.roleTitle.value.trim()||null,consentAccepted:form.consent.checked,communicationConsent:true,source:'demo_request'};const start=await fetch('/bff/public/diagnostic/start',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!start.ok)throw new Error('Não foi possível registrar a solicitação. Revise os dados e tente novamente.');const lead=await start.json();const contact=await fetch('/bff/public/contact-requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({leadId:lead.leadId,sessionId:lead.sessionId,requestType:'contact',requestedPlan:null,notes:form.message.value.trim()||'Demonstração solicitada pelo portal.'})});if(!contact.ok)throw new Error('A solicitação não pôde ser concluída. Tente novamente.');form.reset();feedback.textContent='Solicitação recebida. Nossa equipe comercial entrará em contato para combinar a demonstração.';}catch(error){feedback.textContent=error.message;}finally{button.disabled=false;}});});
