const {read,must}=require('./legacy-public-submit-validator-common');const a=read('app.js'),fn=read('functions/index.js');must('queued status message exists',/Seu resultado também será enviado para seu e-mail/.test(a));must('failed_non_blocking message exists',/failed_non_blocking|envio de e-mail não foi concluído/.test(a));must('sendResultEmail called for resend',/sendResultEmail\(el\.dataset\.id/.test(a));must('getPublicResult returns resultEmail',/resultEmail:/.test(fn));
