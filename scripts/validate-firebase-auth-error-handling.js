#!/usr/bin/env node
const {read,ok,done}=require('./validate-helper');const repo=read('firebase-repository.js');['invalid-login-credentials','user-not-found','wrong-password','invalid-email','user-disabled','too-many-requests','network-request-failed','api-key-not-valid','operation-not-allowed'].forEach(c=>ok(repo.includes(c),`código auth ausente: ${c}`));['E-mail ou senha inválidos.','Usuário desativado.','Muitas tentativas. Aguarde alguns minutos.','Serviço de autenticação indisponível.','Cadastro incompleto. Solicite liberação ao administrador.'].forEach(m=>ok(repo.includes(m)||read('app.js').includes(m)||read('firebase-repository.js').includes('Seu usuário ainda não possui perfil configurado'),`mensagem ausente: ${m}`));done('validate-firebase-auth-error-handling');
