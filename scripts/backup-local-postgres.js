#!/usr/bin/env node
const fs=require('fs'); const {run,writeJson}=require('./live-gate-utils'); fs.mkdirSync('reports/backups',{recursive:true}); const file=`reports/backups/local-postgres-${Date.now()}.sql`; const r=run('docker',['exec','valora-postgres','pg_dump','-U','valora','-d','valoradb','--schema=valorapesquisa']); if(r.status===0) fs.writeFileSync(file,r.stdout); writeJson('reports/local-postgres-backup.json',{generatedAt:new Date().toISOString(),status:r.status===0?'PASS':'FAIL',file:r.status===0?file:null,durationMs:r.durationMs,stderr:r.stderr}); if(r.status!==0) process.exit(r.status); console.log(file);
