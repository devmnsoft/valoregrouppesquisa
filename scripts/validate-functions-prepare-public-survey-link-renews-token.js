const {read,assert}=require('./legacy-final-validator-lib');const s=read('functions/index.js');const i=s.indexOf('exports.preparePublicSurveyLink=');assert(i>=0,'preparePublicSurveyLink missing');const part=s.slice(i,i+1800);assert(/createTokenPair\(\)/.test(part),'must create token pair');assert(/addDaysTimestamp\(180\)/.test(part),'must renew for 180 days');assert(/publicTokenHash\s*:\s*pair\.tokenHash/.test(part),'must save token hash');assert(/buildPublicSurveyUrl\(surveyId,pair\.token/.test(part),'must return URL with raw token');console.log('ok');
