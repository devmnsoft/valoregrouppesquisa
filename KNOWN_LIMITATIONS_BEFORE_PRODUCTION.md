# Limitações conhecidas antes de produção

Alguns itens administrativos do legado ainda são gaps controlados e aparecem como `em implantação`. Testes Playwright dependem de fixture/local server configurado pelo ambiente de CI.
