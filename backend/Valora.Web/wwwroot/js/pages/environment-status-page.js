function safe(v,f){return v===undefined||v===null||Number.isNaN(v)?(f||'—'):String(v)} function err(e){const m=formatFriendlyError(e); $('.error-state').removeClass('d-none').text(m); Toast.error(m)}
$(async function(){if(!$('[data-page="environment-status-page"]').length)return; const checks=[['API',HealthApi.all],['Database',HealthApi.database],['Logging',HealthApi.logging],['Migration',HealthApi.migration],['Version',HealthApi.version]]; try{Loading.show('Verificando ambiente...'); const cards=await Promise.all(checks.map(async c=>{const t=Date.now(); try{const d=await c[1](); return `<div class='col-md-6 col-xl-4'><div class='card h-100'><div class='card-body'><h2>${c[0]}</h2><p class='text-success'>Disponível</p><p>Tempo de resposta: ${Date.now()-t}ms</p><p>Última checagem: ${new Date().toLocaleString()}</p><p>${safe(d.version||d.status,'OK')}</p></div></div></div>`}catch(e){return `<div class='col-md-6 col-xl-4'><div class='card h-100 border-warning'><div class='card-body'><h2>${c[0]}</h2><p class='text-warning'>Falha controlada</p><p>Tempo de resposta: ${Date.now()-t}ms</p><p>CorrelationId: ${safe(e.correlationId)}</p></div></div></div>`}})); $('[data-results]').html(cards.join('')); $('.empty-state').addClass('d-none');}finally{Loading.hide();}});
