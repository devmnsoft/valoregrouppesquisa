#!/usr/bin/env node
const fs=require('fs'),path=require('path');const terms=['resp'+'_demo','demo'+'_response','response'+'_demo','pending-provider'+'@local.invalid'];const allowed=f=>/\.(test|spec)\./.test(f)||f.endsWith('.md');const dirs=['.'];const skip=/node_modules|\.git|dist|reports/;const hits=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(skip.test(p))continue;if(e.isDirectory())walk(p);else if(/\.(js|cs|cshtml|json|bat|yml|yaml|html)$/.test(p)&&!allowed(p)){const t=fs.readFileSync(p,'utf8');for(const term of terms)if(t.includes(term))hits.push(`${p}: contém ${term}`)}}}walk('.');if(hits.length){console.error(hits.join('\n'));process.exit(1)}console.log('validate-no-demo-response-email-flow: PASS')
