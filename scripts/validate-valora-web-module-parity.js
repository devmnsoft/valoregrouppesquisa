#!/usr/bin/env node
const fs=require('fs'); const txt=fs.readFileSync('backend/Valora.Web/wwwroot/js/security/module-definitions.js','utf8'); const modules='clientes financeiro planos modulos usuarios funcionarios formularios pesquisas convitesEmail respostas relatorios certificados actionPlans valorabot support lgpd integrations exportacoes benchmark whiteLabel backup logs'.split(' '); const fields='id label description scope requiredPermission defaultEnabled commercialFeature route'.split(' '); const missing=[...modules,...fields,'ValoraModules','canUseModule'].filter(x=>!txt.includes(x)); if(missing.length){console.error('module parity missing: '+missing.join(', '));process.exit(1);} console.log('validate-valora-web-module-parity: PASS');
