const {read,assertIncludes}=require('./validate-public-result-empty-rt-common');const s=read('app.js');assertIncludes(s,'function isExternalBrowserNoise(error)','browser noise detector required');assertIncludes(s,'recordExternalBrowserNoise(reason);e.preventDefault();return false;','unhandledrejection must suppress external browser noise');console.log('OK external browser noise handled');
