$(function(){const form=document.querySelector('[data-page="forgot-password-page"] form'); if(!form)return; form.addEventListener('submit',async e=>{e.preventDefault();$('.error-state,.success-state').addClass('d-none').text(''); if(!form.checkValidity()){form.classList.add('was-validated');return;} try{Loading.show('Enviando instruções...'); await AuthApi.forgotPassword({email:form.email.value.trim()}); $('.success-state').removeClass('d-none').text('Se o e-mail estiver cadastrado, as instruções serão enviadas.'); Toast.success('Solicitação recebida.');}catch(error){const msg=formatFriendlyError(error); $('.error-state').removeClass('d-none').text(msg); Toast.error(msg);}finally{Loading.hide();}});});
