#!/usr/bin/env node
const fs=require('fs'); const path=require('path'); const bad=['Organização autenticada','contato@valora.local','plan = "standard"','score = 0','level = "controlado"','Array.Empty<object>()','Dados carregados com renderização específica']; function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name);return e.isDirectory()?walk(p):[p]});} const hits=[]; for(const f of walk('backend')){if(!/\.(cs|cshtml|js)$/.test(f))continue; const t=fs.readFileSync(f,'utf8'); for(const b of bad) if(t.includes(b)) hits.push(`${f}: ${b}`);} if(hits.length){console.error(hits.join('\n')); process.exit(1);} console.log('validate-valora-web-no-fake-admin-data: PASS');
