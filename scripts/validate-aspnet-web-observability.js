const fs=require('fs'); const f='ASPNET_WEB_OBSERVABILITY.md'; if(!fs.existsSync(f)) throw new Error(`${f} ausente`); const d=fs.readFileSync(f,'utf8'); ['logs do Valora.Web','logs da Valora.Api','correlationId','IIS','Docker','500','API offline','CORS','token expirado'].forEach(t=>{if(!d.toLowerCase().includes(t.toLowerCase())) throw new Error(`Observabilidade sem ${t}`)}); console.log('validate-aspnet-web-observability: PASS');
