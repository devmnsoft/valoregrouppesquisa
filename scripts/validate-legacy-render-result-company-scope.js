const fs=require('fs');const s=fs.readFileSync('app.js','utf8');const start=s.indexOf('async function renderResult');const end=s.indexOf('function isDemoCompany',start);if(start<0||end<0)throw new Error('renderResult não localizado');const r=s.slice(start,end);for(const x of ['normalizePublicResultBundle','normalizePublicResultViewModel','const bundle=','bundle.company','safeCertificateHtml(bundle.response,bundle.survey,bundle.form,bundle.company)'])if(!r.includes(x))throw new Error('renderResult sem '+x);if(/safeCertificateHtml\(r,s,formForInsight,company\)/.test(r))throw new Error('company legado usado diretamente');console.log('legacy render result company scope: PASS');
