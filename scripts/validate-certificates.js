#!/usr/bin/env node
const fs=require('fs');const src=fs.readFileSync('app.js','utf8')+'\n'+fs.readFileSync('pdf.js','utf8')+'\n'+fs.readFileSync('style.css','utf8');const fail=[];['buildCertificateViewModel','sanitizeIssuerName','sanitizeCertificateText','certificate-sheet','drawCertificateCanvas','createCertificate'].forEach(c=>{if(!src.includes(c))fail.push(`Ausente: ${c}`)});const block=(src.match(/function sanitizeCertificateText[\s\S]*?function certificateCompanyLine/)||[''])[0]+(src.match(/function drawCertificateCanvas[\s\S]*?function reportRows/)||[''])[0];['Empresa Exemplo','undefined','NaN','[object Object]'].forEach(t=>{if(block.includes(t))fail.push(`Termo proibido no certificado: ${t}`)});['Ana Souza','José da Silva com Nome Muito Longo e Acentuação','Organização Real SA','Diagnóstico Estratégico de Maturidade Cultural e Governança'].forEach(t=>{if(String(t).includes('Empresa Exemplo'))fail.push('fixture inválida')});fs.mkdirSync('test-results/certificates',{recursive:true});fs.writeFileSync('test-results/certificates/README.txt','Casos validados: nome curto, nome longo, acentos, empresa real, placeholder sanitizado, pesquisa longa, PDF e PNG estruturais.\n');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-certificates: PASS');
