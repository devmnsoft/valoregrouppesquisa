#!/usr/bin/env node
const fs=require('fs');const vm=require('vm');const src=fs.readFileSync('config.js','utf8');const sandbox={window:{}};vm.createContext(sandbox);vm.runInContext(src,sandbox);const cfg=sandbox.window.ValoraConfig||{};const fc=cfg.FIREBASE_CONFIG||{};const fail=[];const need=['apiKey','authDomain','projectId','storageBucket','messagingSenderId','appId'];for(const k of need)if(!String(fc[k]||'').trim())fail.push(`${k} ausente`);if(cfg.RUNTIME_ENV!=='production')fail.push('RUNTIME_ENV deve ser production');if(cfg.STORAGE_MODE!=='firebase')fail.push('STORAGE_MODE deve ser firebase');if(cfg.FIREBASE_ENABLED!==true)fail.push('FIREBASE_ENABLED deve ser true');if(cfg.FIREBASE_PLAN!=='spark')fail.push('FIREBASE_PLAN deve ser spark');if(cfg.ENABLE_CLOUD_FUNCTIONS!==false)fail.push('Cloud Functions não devem ser obrigatórias no Spark');if(cfg.REQUIRE_AUTH_SERVER_VALIDATION!==false)fail.push('REQUIRE_AUTH_SERVER_VALIDATION deve ser false');if(cfg.LOCAL_API_ENABLED!==false)fail.push('LOCAL_API_ENABLED deve ser false');if(fc.projectId!=='gestordepesquisa')fail.push('projectId deve ser gestordepesquisa');if(!['external-api','disabled'].includes(cfg.EMAIL_TRANSPORT))fail.push('EMAIL_TRANSPORT inválido');if(/Firebase não configurado|FIREBASE_CONFIG incompleto|apiKey ausente|appId ausente|messagingSenderId ausente/.test(src))fail.push('config.js contém mensagem bloqueadora de Firebase incompleto');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-firebase-config: PASS');
