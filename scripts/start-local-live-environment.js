const {run,assert}=require('./live-gate-utils');
(async()=>{assert(!/production/i.test(process.env.NODE_ENV||''),'local live não roda com NODE_ENV=production'); const dc=run('docker',['compose','-f','docker-compose.postgres.yml','up','-d']); if(dc.status!==0){console.error(dc.stderr||dc.stdout); process.exit(dc.status||1);} const api=process.env.VALORA_API_BASE_URL||process.env.API_BASE_URL; if(api){ for(let i=0;i<30;i++){try{const r=await fetch(api.replace(/\/$/,'')+'/health'); if(r.status<500){console.log('local live API pronta'); process.exit(0)}}catch{} await new Promise(r=>setTimeout(r,1000));} console.error('API configurada não ficou pronta'); process.exit(1);} console.log('PostgreSQL local iniciado. API_BASE_URL não definido; inicie a API ou informe VALORA_API_BASE_URL.');})();
