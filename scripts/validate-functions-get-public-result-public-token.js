const {read,assert}=require('./legacy-final-validator-lib');const s=read('functions/index.js');const m=s.match(/exports\.getPublicResult[\s\S]*?exports\./);assert(m&&!/authedUser|requireAdminUser/.test(m[0]),'getPublicResult must not require auth');assert(/verify(ResultToken|BearerToken)[\s\S]*resultToken/.test(m[0]),'getPublicResult must verify result token');console.log('ok');
