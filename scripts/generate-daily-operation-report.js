const fs=require('fs');const report={generatedAt:new Date().toISOString(),checks:['config live','Functions','SMTP','fila de e-mail','dead-letter','pesquisa gratuita','link público','erros públicos','menu mobile','custo/uso Blaze','backups','logs LGPD'],status:'manual_review_required'};fs.mkdirSync('reports',{recursive:true});fs.writeFileSync('reports/daily-operation-report.json',JSON.stringify(report,null,2));fs.writeFileSync('DAILY_OPERATION_REPORT.md',`# Relatório Diário de Operação\n\nGerado em: ${report.generatedAt}\n\n${report.checks.map((c,i)=>`${i+1}. ${c}: revisão manual assistida`).join('\n')}\n`);console.log('generate-daily-operation-report: PASS');
