const fs=require('fs');const app=fs.readFileSync('app.js','utf8');const fail=[];['communicationsSection','Data</th><th>Empresa</th><th>Pesquisa</th><th>Participante</th><th>E-mail</th><th>Canal</th><th>Status</th><th>Tentativas</th><th>Último erro</th><th>Ações','resendResultEmail','copyText','viewResponse','role===\'participante\'','/communication/email/test'].forEach(t=>{if(!app.includes(t))fail.push(`Admin comunicação ausente: ${t}`)});['SMTP_PASS','SMTP_PASSWORD','FIREBASE_PRIVATE_KEY','private_key'].forEach(t=>{if(app.includes(t))fail.push(`segredo exposto no frontend: ${t}`)});if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-communication-admin: PASS');
