const {read,must}=require('./legacy-final-validator-common');const a=read('app.js'),f=read('functions/index.js');must('submit success guards missing token',/if\(!responseId\|\|!resultToken\)/.test(a));must('publicResultUrl throws on missing resultToken',/if\(!resultToken\)throw/.test(f));must('getPublicResult requires token',/missing_result_token/.test(f));console.log('legacy result token required: PASS');
