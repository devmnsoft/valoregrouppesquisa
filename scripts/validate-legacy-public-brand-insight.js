const {read,must}=require('./legacy-final-validator-common');const a=read('app.js'),p=read('pdf.js');const pub=a.slice(a.indexOf('function renderImmediateResultAfterSubmit'),a.indexOf('function certificateHtml'));must('PUBLIC_PRODUCT_NAME missing',/PUBLIC_PRODUCT_NAME='Valora Insight™'/.test(a));must('public result must not show Valora Pulse',!pub.includes('Valora Pulse™'));must('pdf uses Valora Insight',p.includes('Valora Insight™'));must('HOME not present in public result',!/\bHOME\b/.test(pub));console.log('legacy public brand insight: PASS');
