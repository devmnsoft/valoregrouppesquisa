const {read,ok,report}=require('./sprint60-utils');const a=read('app.js'),fns=read('functions/index.js');let f=[];['repairFreeSurveyPublicLink','admin_valora','confirm(','publicToken','publicUrl','publicLink','expiresAt','visibleOnHome','featuredOnHome','status:\'active\'','auditLog','maskSensitiveToken'].forEach(x=>ok((a+fns).includes(x),`missing ${x}`,f));ok(!/tokenHash.*publicUrl/.test(fns),'tokenHash used as URL token',f);report('validate-admin-free-survey-repair-flow',f);
