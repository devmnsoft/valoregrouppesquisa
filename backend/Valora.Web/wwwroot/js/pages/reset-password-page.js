document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('[data-page="reset-password-page"] form');if(!form)return;form.addEventListener('submit',async event=>{event.preventDefault();const error=document.querySelector('.error-state'),success=document.querySelector('.success-state');error.classList.add('d-none');if(form.password.value.length<12||form.password.value!==form.confirmPassword.value){error.textContent='Use ao menos 12 caracteres e confirme a mesma senha.';error.classList.remove('d-none');return;}try{await AuthApi.resetPassword({token:form.token.value.trim(),newPassword:form.password.value,email:new URLSearchParams(location.search).get('email')||''});success.textContent='Senha redefinida. Entre novamente.';success.classList.remove('d-none');setTimeout(()=>location.assign('/Account/Login'),1200);}catch(ex){error.textContent=ex.message;error.classList.remove('d-none');}});});
