# Pendências reais

- Executar o script em PostgreSQL homologado (banco existente duas vezes e banco vazio) e registrar evidências operacionais.
- Concluir a criação de diagnóstico diretamente a partir de `form_templates` e o snapshot integral no ciclo; nesta entrega o snapshot oficial do template é persistido, mas o catálogo operacional antigo ainda requer integração.
- Materializar recomendações e resultados dimensionais no repositório transacional após submissão; o cálculo real e a prévia autenticada estão disponíveis.
- Ligar os previews específicos aos módulos operacionais de Certificates e Executive Report e validar configuração de PDF.
- Executar smoke autenticado completo (login, publicação, resposta, notificações, auditoria e Workspace) em ambiente com PostgreSQL e credenciais.
