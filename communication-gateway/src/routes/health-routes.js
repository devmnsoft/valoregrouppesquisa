const express=require('express');const {config}=require('../config');const router=express.Router();router.get('/health',(_,res)=>res.json({ok:true,service:'valora-communication-gateway',environment:config.environment,time:new Date().toISOString()}));module.exports=router;
