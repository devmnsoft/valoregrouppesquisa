# Fluxo de cadastro de cliente

O cadastro cria organização em `organizations`, define plano padrão quando necessário, status, slug, nomes públicos/legais, assinatura inicial e auditoria. A listagem admin consome `state.companies` normalizado a partir de `organizations`.
