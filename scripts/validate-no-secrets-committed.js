#!/usr/bin/env node
const fs=require('fs'),path=require('path');const exts=new Set(['.js','.json','.md','.env','.bat','.yml','.yaml']);const ignore=new Set(['node_modules','.git','reports','backups']);let bad=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(ignore.has(e.name))continue;const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(exts.has(path.extname(e.name))){const s=fs.readFileSync(p,'utf8');const pats=[/private_key\s*[:=]/i,/ya29\.[0-9A-Za-z_-]{20,}/,/resultToken\s*[:=]\s*['"][A-Za-z0-9_-]{20,}['"]/i,/tokenHash\s*[:=]\s*['"][a-f0-9]{32,}['"]/i];pats.forEach(r=>{if(r.test(s))bad.push(`${p}: ${r}`)});}}}walk(process.cwd());if(bad.length){console.error('Segredos potenciais encontrados:\n'+bad.join('\n'));process.exit(1)}console.log('OK: nenhum segredo proibido encontrado.');
