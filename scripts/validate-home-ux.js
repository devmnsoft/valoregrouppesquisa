#!/usr/bin/env node
const fs=require('fs');const path=require('path');const root=path.resolve(__dirname,'..');const files=['app.js','style.css','config.js','log-service.js'];const text=Object.fromEntries(files.map(f=>[f,fs.readFileSync(path.join(root,f),'utf8')]));const all=Object.values(text).join('\n');const checks=[];function check(name,ok,details=''){checks.push({name,ok,details});}
['home','hero','faq','plans'].forEach(c=>check(`Estrutura ${c}`,new RegExp(c,'i').test(all),`${c} encontrada`));
check('Função getFeaturedFreeSurvey',/function\s+getFeaturedFreeSurvey\s*\(/.test(text['app.js']));check('Função buildPublicSurveyUrl',/function\s+buildPublicSurveyUrl\s*\(/.test(text['app.js']));check('CTA diagnóstico grátis',/Responder diagnóstico grátis/.test(all));check('Plano grátis público',/Grátis|Gratis|free/.test(all));check('Valora Insight público',/Valora Insight™/.test(all));['5 dimensões','25 perguntas','125 pontos'].forEach(t=>check(`Texto público ${t}`,all.includes(t)));
const homeSegment=text['app.js'].match(/function renderHome\(\)[\s\S]*?function officialPublicPricingPlans/)?.[0]||text['app.js'];['Blaze','Cloud Functions','Firebase','Spark','PRD','users/{uid}','README','undefined','NaN','[object Object]'].forEach(t=>check(`Home pública sem ${t}`,!homeSegment.includes(t)));check('Home sem Empresa Exemplo',!homeSegment.includes('Empresa Exemplo'));
check('Cloud Functions desabilitadas',/ENABLE_CLOUD_FUNCTIONS\s*:\s*false/.test(text['config.js']));check('CTA público usa link de pesquisa',/buildPublicSurveyUrl|featuredSurveyLink|Responder diagnóstico grátis/.test(homeSegment));
const failed=checks.filter(c=>!c.ok);for(const c of checks)console.log(`${c.ok?'PASS':'FAIL'} ${c.name}${c.details?` - ${c.details}`:''}`);if(failed.length){console.error(`\n${failed.length} validação(ões) falharam.`);process.exit(1);}console.log('\nHome UX validada com sucesso.');
