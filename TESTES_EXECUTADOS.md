# Testes executados

- `node scripts/validate-repository-logging.js` — passou.
- `node scripts/validate-no-validator-fake-comments.js` — passou.
- `dotnet build backend/Valora.sln` — não executado no ambiente atual porque o comando `dotnet` não está instalado.
