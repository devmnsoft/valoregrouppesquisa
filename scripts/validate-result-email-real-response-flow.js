#!/usr/bin/env node
const fs=require('fs');const txt=['app.js','api-repository.js','backend/Valora.Web/wwwroot/js/api/communications-api.js','backend/Valora.Web/wwwroot/js/pages/result-page.js'].filter(fs.existsSync).map(f=>fs.readFileSync(f,'utf8')).join('\n');const fail=[];['/communications/result/','send-email','encodeURIComponent(responseId)','resultToken','includeCertificate','participant','correlationId','failed-network','pending-retry'].forEach(x=>{if(!txt.includes(x))fail.push(`ausente: ${x}`)});if(new RegExp('resp'+'_demo').test(txt))fail.push('fluxo contém demo response id');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-result-email-real-response-flow: PASS')
