const {read,must}=require('./legacy-public-submit-validator-common');const f=read('functions/index.js');for(const code of ['participant_required','lgpd_required','required_question_missing','invalid_public_token','survey_unavailable','survey_not_found','form_not_found','internal_submit_error'])must(`${code} has details`,f.includes(code));must('friendlyMessage is returned',/friendlyMessage/.test(f));must('unexpected error has correlationId',/correlationId/.test(f));
