const fs=require('fs');const txt=fs.readFileSync('app.js','utf8')+'\n'+fs.readFileSync('config.js','utf8');const req=['WHATSAPP_CONTACT_URL','Seu diagnóstico gratuito foi concluído.','Falar com especialista Valora','Este resultado mostra uma primeira leitura da maturidade da sua organização'];const fail=req.filter(x=>!txt.includes(x)).map(x=>`ausente: ${x}`);if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-free-survey-whatsapp-cta: PASS')
