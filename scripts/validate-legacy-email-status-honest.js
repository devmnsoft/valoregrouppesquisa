const {read,assertIncludes}=require('./validate-public-result-empty-rt-common');const s=read('app.js');assertIncludes(s,'function renderEmailDeliveryStatus(status,hasResultToken=true)','email status must consider result token');assertIncludes(s,'Resultado registrado, mas o link de acesso não foi gerado corretamente.','must show incomplete link message');assertIncludes(s,"if(value==='queued'||value==='pending'||value==='pending_retry')return 'Seu resultado foi registrado e o envio por e-mail está em processamento.'",'queued must not say sent');console.log('OK honest email status');
