const fs=require('fs');const s=fs.readFileSync('functions/index.js','utf8');const fn=s.slice(s.indexOf('function normalizeSendMailResult'),s.indexOf('function publicResultUrl'));for(const x of ['messageId&&acceptedTarget&&!rejectedTarget','status:\'sent\',sent:true,queued:false','reason:\'smtp_not_accepted\''])if(!fn.includes(x))throw new Error('normalização SMTP insegura: '+x);console.log('functions no false email sent: PASS');
