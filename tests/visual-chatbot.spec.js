const {test,expect}=require('@playwright/test');
test('ValoraBot responde planos e suporte',async({page})=>{await page.goto('/index.html#home');await page.locator('[data-action="toggleBot"]').first().click();await expect(page.locator('#botPanel')).toBeVisible();await page.locator('form[data-form="bot"] input[name="message"]').fill('quais são os planos?');await page.locator('form[data-form="bot"] button').click();await expect(page.locator('#botPanel')).toContainText('Profissional');await expect(page.locator('#botPanel')).toContainText('Falar com atendente');await page.screenshot({path:'test-results/screenshots/chatbot-planos.png',fullPage:true});});
