const fs=require('fs');const txt=['pdf.js','backend/Valora.Application/Certificates/CertificateService.cs','backend/Valora.Web/Views/Certificates/Details.cshtml'].filter(fs.existsSync).map(f=>fs.readFileSync(f,'utf8')).join('\n');const req=['Valora Pulse','Valora Group','Certificado','Diagnóstico','participante','pontuação','maturidade','valida','LGPD'];const bad=['Empresa Exemplo','[object Object]'];const fail=req.filter(x=>!new RegExp(x,'i').test(txt)).map(x=>`certificado sem ${x}`);bad.forEach(x=>{if(txt.includes(x))fail.push(`certificado contém ${x}`)});if(/undefined|NaN/.test(txt))fail.push('certificado contém undefined/NaN literal');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-certificate-rich-content: PASS')
