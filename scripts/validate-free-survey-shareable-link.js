const fs=require('fs');const txt=['app.js','config.js','config/config.production.js'].map(f=>fs.readFileSync(f,'utf8')).join('\n');const fail=[];['FREE_SURVEY_LINK_EXPIRATION_DAYS',': 3650','searchParams.set(\'survey\'','searchParams.set(\'token\'','searchParams.set(\'org\'','publicUrl','publicLink'].forEach(x=>{if(!txt.includes(x))fail.push(`ausente: ${x}`)});if(/tokenHash/.test((txt.match(/function buildHomeFeaturedSurveyUrl[\s\S]*?\n}/)||[''])[0]))fail.push('link público usa tokenHash');if(fail.length){console.error(fail.join('\n'));process.exit(1)}console.log('validate-free-survey-shareable-link: PASS')
