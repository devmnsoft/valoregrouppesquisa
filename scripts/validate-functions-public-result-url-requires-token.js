const {read,assertIncludes}=require('./validate-public-result-empty-rt-common');const s=read('functions/index.js');assertIncludes(s,'function publicResultUrl(responseId,resultToken){if(!responseId)','publicResultUrl must require responseId');assertIncludes(s,"code:'missing_result_token'",'publicResultUrl must require resultToken');assertIncludes(s,"url.searchParams.set('rt',resultToken)",'publicResultUrl must write rt from token');console.log('OK publicResultUrl requires token');
