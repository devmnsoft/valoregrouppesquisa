function safe(v,f){return v===undefined||v===null||Number.isNaN(v)?(f||'—'):String(v)} function err(e){const m=formatFriendlyError(e); $('.error-state').removeClass('d-none').text(m); Toast.error(m)}
$(async function(){if(!$('[data-page]').filter((_,e)=>$(e).data('page')==='migration-page').length)return; try{Loading.show('Consultando API...'); const data=await MigrationApi.status(); $('[data-results]').html(`<div class='col-12'><div class='card card-body'><h2>Dados disponíveis</h2><p>${safe(Array.isArray(data)?data.length+' registros':data.status||data.message,'Consulta concluída pela API.')}</p></div></div>`); $('.empty-state').addClass('d-none');}catch(e){err(e)}finally{Loading.hide();}});
