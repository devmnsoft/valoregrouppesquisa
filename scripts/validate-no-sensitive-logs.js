const fs=require('fs'),path=require('path');const files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['.git','node_modules','dist','bin','obj'].includes(e.name))continue;const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.(cs|js)$/.test(e.name))files.push(p)}} walk('.');
const bad=[];for(const p of files){const s=fs.readFileSync(p,'utf8').split(/\r?\n/);s.forEach((l,i)=>{if(/Log(?:Information|Warning|Error)\([^\n]*(resultToken|result_token_hash|private_key|smtpPassword|connectionString|password|senha)/i.test(l)&&!/LogSanitizer|Sanitize|HasToken|Status|Length/.test(l))bad.push(`${p}:${i+1}`);});} if(bad.length){console.error('Possíveis logs sensíveis:\n'+bad.join('\n'));process.exit(1)} console.log('Logs sensíveis validados.');
