const {read,must}=require('./legacy-final-validator-common');const f=read('functions/index.js');must('transporter verify missing',/await transporter\.verify\(\)/.test(f));must('sendMail missing',/await transporter\.sendMail/.test(f));must('accepted/messageId validation missing',/messageId&&acceptedTarget&&!rejectedTarget/.test(f));must('failed_non_blocking missing',/failed_non_blocking/.test(f));console.log('functions email real status: PASS');
