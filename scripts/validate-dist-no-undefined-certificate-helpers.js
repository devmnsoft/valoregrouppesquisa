const fs=require('fs');const path=require('path');const dir='dist/assets';if(!fs.existsSync(dir)){console.log('dist ausente; rode build:prod antes da validação final.');process.exit(0);}const files=fs.readdirSync(dir).filter(f=>/^app.*\.js$/.test(f));if(!files.length)throw new Error('dist/assets/app*.js não encontrado');for(const f of files){const s=fs.readFileSync(path.join(dir,f),'utf8');if(s.includes('dimensionRecommendation(')&&!s.includes('function dimensionRecommendation'))throw new Error(f+' chama dimensionRecommendation sem função');}console.log('dist no undefined certificate helpers: PASS');
