# PostgreSQL local

```powershell
docker compose -f docker-compose.postgres.yml up -d
docker ps
```

Conexão padrão: `Host=localhost;Port=5434;Database=valoradb;Username=valora;Password=valora_dev_123`.
