# Cadastro de usuário via Firebase Auth

O fluxo legado chama `createCompanyUser` quando não há UID/id, garantindo Firebase Auth, `users/{uid}`, custom claims, role, companyId, status e link de redefinição.
